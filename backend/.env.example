# ============================================================
# Environment Variables – copy this file to .env and fill in
# ============================================================

# Secret key for JWT signing – generate with:
#   python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CHANGE_THIS_TO_A_SECURE_SECRET_KEY_32_CHARS_MIN

# Admin account seeding (used only by seed_db.py)
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=StrongP@ssw0rd
